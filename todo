Refactoring:
    use ... macro in xopen()
    set the default handler for SIGTTOU for child processes

Features:
    comments #
    errors when duplicating redirects
    interactive
    xmalloc
    write tests
